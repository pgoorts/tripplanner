Phase: 4
Step: mockup images - designmockups published, pending developer confirmation
Current: Phase 2 implementation plan is complete (Blocks 1-5, including publishing manual).
Phase 3 (Settings, Sync Visibility, Richer Notes & Events): description_detail.txt, techstack.txt,
and datastructure.txt all confirmed by developer. For the mockup step, this environment had no
direct image-generation tool (unlike Phase 1), so used the /design skill's canvas instead: 7
screen mockups (Global Settings, Trip Settings, Opened Trip w/ sync bar + delete-confirm dialog,
Add Note w/ Pass type + simplified Link, Pkpass wallet rendering, Add Event for Flight, Event list
w/ custom Flight card) built to match the real app's Material3 palette/typeface (read from
Color.kt/Type.kt and the existing screens' Compose source). Source files + canvas.json dropped in
DesignDocs/Phase3/designmockups/ (see its README.txt); published, exportable canvas at
https://claude.ai/code/artifact/4d0c8789-3d9d-4137-be18-ffe875e3c4e2. Background review found and
fixed 4 issues (off-palette colors in 3 files, a departure/arrival split-card content bug); user
also asked to drop the "NEW" badge on the Pass note type in the mockup, done. Mockups confirmed
by developer.

Step: implementation - Block 3 code complete, pending developer device verification
implementation_plan.txt (5 blocks, see its contents) confirmed by developer; implementation
started.

Block 1 (Build Environment, New Deps & Schema Scaffolding) [COMPLETE]:
- Added zxing:core:3.5.3 and firebase-storage-ktx to App/app/build.gradle.kts.
- Added nullable defaultTimezone to TripEntity; nullable flightNumber/departureAirportCode/
  arrivalAirportCode/bookingNumber to EventEntity; PKPASS to NoteType; nullable local-only
  localAttachmentPath to NoteEntity.
- Bumped AppDatabase to version 2, added MIGRATION_1_2 (ALTER TABLE ADD COLUMN for all the above)
  in AppDatabase.kt, wired via .addMigrations() in DatabaseModule.kt (the builder previously had
  no Migration/fallback at all).
- Adding the PKPASS enum value broke 4 exhaustive `when(NoteType)` blocks (OpenedEventScreen.kt,
  OpenedNoteScreen.kt x2, OpenedTripScreen.kt) — added minimal placeholder branches (a
  Teal300/CreditCard visual, and a "Pass rendering coming soon" text in the note editor) so the
  build compiles; Block 5 replaces these with the real pkpass UI.
- `./gradlew assembleRelease` succeeds from a clean baseline through all these changes.


Developer confirmed on-device: installed over an existing Phase-2 install, no crash/data loss
(migration ran), and confirmed no visible behavior change apart from the expected placeholder
Pkpass note type. Step 1.4 fully verified. Block 1 complete.

Block 2 (Global & Per-Trip Settings: Timezone Defaults, Sync Interval) [COMPLETE]:
- Added `PreferencesRepository` (data/repository/PreferencesRepository.kt) wrapping the existing
  `user_prefs` DataStore with `defaultTimezone` (String?), `syncIntervalMinutes` (Int, default 15),
  `lastSuccessfulSyncAt` (Long?, written starting Block 3) — per datastructure.txt §6. Made
  `UserSessionManager`'s `Context.dataStore` extension `internal` (was `private`) so this new
  repository can share the same DataStore instance/file instead of opening a second, conflicting
  one.
- Added `SyncScheduler` (sync/SyncScheduler.kt) wrapping the periodic `SyncWorker` WorkManager
  enqueue (`schedulePeriodic(intervalMinutes, policy)`, default policy KEEP). `TripPlannerApplication`
  now reads the persisted interval (via Hilt-injected `PreferencesRepository`) on startup and
  schedules with KEEP (preserves an already-running schedule across app restarts, replacing the old
  hardcoded 15-minute request); Settings calls it with `REPLACE` on an explicit user change.
- Renamed the Profile screen/viewmodel to Settings: `ui/profile/{ProfileScreen,ProfileViewModel}.kt`
  deleted, replaced by `ui/settings/{SettingsScreen,SettingsViewModel}.kt` (same Inner Circle /
  Packing Template sections, unchanged), plus a new "Preferences" section (Default timezone row
  opening a search picker; Sync interval as 5/15/30/60-minute chips) matching designmockups/Main.dc.html.
  `MainActivity`'s `Screen.Profile` route -> `Screen.Settings` ("settings"); `HomeScreen`'s
  `onProfileClick` -> `onSettingsClick` (same Person-icon avatar entry point, just relabeled).
- Added a reusable `TimezonePickerDialog` (ui/components/TimezonePickerDialog.kt) — a modal,
  search-filtered list over `ZoneId.getAvailableZoneIds()` — used by both the Settings screen and
  the new per-trip settings dialog below.
- Added a Trip Settings entry point (gear icon in `OpenedTripScreen`'s top bar) opening
  `TripSettingsDialog`, matching designmockups/TripSettings.dc.html: shows the trip's
  `defaultTimezone` override (or "Using global default: X" / "Not set"), a tap-to-open
  `TimezonePickerDialog`, and a "Clear override" action. Writes go through
  `OpenedTripViewModel.setTripDefaultTimezone()` -> `TripRepository.updateTrip()` (existing
  PENDING_UPDATE path).
- Wired timezone precedence into the Add Event form: `OpenedTripViewModel` exposes
  `globalDefaultTimezone` (from `PreferencesRepository`); `OpenedTripScreen` resolves
  `trip.defaultTimezone ?: globalDefaultTimezone ?: "UTC"` and passes it as `AddEventDialog`'s
  initial timezone. `AddEventDialog` gained a Timezone field (tap to open the same
  `TimezonePickerDialog`) — previously the dialog had no timezone field at all and events were
  silently created with the "UTC" repository default.
- `./gradlew assembleRelease` succeeds from a clean baseline through all these changes.

Developer confirmed on-device (Step 2.4): timezone precedence pre-fill (global default -> event;
trip override -> event; clear override -> falls back to global) and the sync-interval preset
change re-enqueueing the periodic job at the new interval. Block 2 complete.

Block 3 (Sync Status Bar & Delete Confirmations) [CODE COMPLETE, pending device check]:
- `SyncWorker` now resolves `PreferencesRepository` via its Hilt entry point and, after both the
  upload and download phases complete without error, writes `lastSuccessfulSyncAt` (epoch millis)
  — covers both the periodic job and the new manual one-off run below.
- `SyncScheduler` gained `isNetworkAvailable()` (ConnectivityManager/NetworkCapabilities check),
  `triggerManualSync()` (one-off `SyncWorker` run via `enqueueUniqueWork("TripPlannerManualSync",
  REPLACE, ...)`, same `NetworkType.CONNECTED` constraint as the periodic job), and
  `observeManualSyncWorkInfo()` (Flow of that unique work's latest `WorkInfo`). Added
  `ACCESS_NETWORK_STATE` permission to the manifest for the connectivity check.
- `OpenedTripViewModel` exposes `lastSuccessfulSyncAt` (StateFlow, from the preferences
  repository) and `syncStatus` (StateFlow<SyncBarStatus> — IDLE/IN_PROGRESS/FAILED), plus
  `triggerManualSync()`. Design choice: rather than relying on `SyncWorker`'s `Result.retry()`
  path to ever reach a terminal `WorkInfo.FAILED` state (it won't — WorkManager just keeps
  retrying with backoff), "no connectivity" is checked up front via
  `syncScheduler.isNetworkAvailable()` before even enqueuing manual-sync work; a positive result
  shows FAILED for 3s then reverts to IDLE. WorkInfo RUNNING/ENQUEUED maps to IN_PROGRESS,
  SUCCEEDED maps back to IDLE (the bar's relative-time text updates automatically off the new
  `lastSuccessfulSyncAt` write).
- `OpenedTripScreen.kt` gained a `SyncStatusBar` (below the trip header, above the tab row, per
  designmockups/OpenedTrip.dc.html): relative-time text via `DateUtils.getRelativeTimeSpanString`
  (special-cased to "Synced just now" under 1 minute, "Not synced yet" if never synced), refreshed
  every 60s via a `LaunchedEffect` ticker, plus a manual-sync icon button (disabled while
  IN_PROGRESS) wired to `viewModel.triggerManualSync()`.
- Added a reusable `ConfirmDeleteDialog` (ui/components/ConfirmDeleteDialog.kt) matching
  designmockups/OpenedTrip.dc.html's dialog (title/message, Cancel + destructive-red Delete).
  Wired in front of every existing delete action, replacing each immediate delete with
  `pendingDelete*` local state that opens the dialog first:
  * `OpenedTripScreen.kt`: Event/Note/Reminder deletes in the Itinerary/Notes/Reminders tabs.
  * `OpenedEventScreen.kt`: Note/Reminder deletes on the event's linked-items lists (same delete
    actions, different screen — not explicitly named in implementation_plan.txt's file list but
    covered for "every existing delete action" per the block's stated goal).
  * `HomeScreen.kt`: Trip delete (both Large and Small trip cards).
  * `SettingsScreen.kt`: Inner Circle contact removal, Packing List Template delete.
  * `OpenedNoteScreen.kt`: checklist item removal (index-based, matching the existing
    `removeChecklistItem(index)` API).
- `./gradlew assembleRelease` succeeds from a clean baseline through all these changes.

Two bugs found and fixed during Step 3.4 device verification:
- Sync bar showed "syncing"/"failed" on plain app/trip open, unrelated to any sync actually in
  progress. Root cause: `OpenedTripViewModel` observed WorkManager's *persisted* state for the
  unique work name `"TripPlannerManualSync"` (`SyncScheduler.observeManualSyncWorkInfo()`), which
  reflects whatever that work name's *last* run (possibly from a prior app session — e.g. a
  previously failed sync, or one left ENQUEUED waiting on connectivity) ended up as. Any new
  screen subscribing to it immediately replayed that stale state. Fixed by having
  `SyncScheduler.triggerManualSync()` return the freshly-built `OneTimeWorkRequest`'s own ID, and
  observing `getWorkInfoByIdFlow(id)` (new `observeSyncWorkInfo(id)`) scoped to that specific
  request instead — the bar now only reflects a sync genuinely happening right now, defaulting to
  IDLE with no eager subscription in `init`.
- Manual sync failed with Firestore `PERMISSION_DENIED` on the trips download query. Root cause:
  `SyncWorker.performDownloadFlow()` built the collaborator-membership query as a dotted string —
  `.whereNotEqualTo("collaborators.$currentUserEmail", null)` — and since email addresses contain
  `.`, Firestore parsed it as nested field segments (`collaborators` → `patrikgoorts@gmail` →
  `com`) instead of the single map key `collaborators["patrikgoorts@gmail.com"]` the security
  rules and upload path actually use. Fixed with
  `FieldPath.of("collaborators", currentUserEmail)`, which treats the email as one literal key.
- Also deployed `firestore.rules` (already existed in the repo root, unrelated to this bug — it
  had just never been pushed) via `firebase deploy --only firestore:rules` after the developer
  created the Cloud Firestore database in the console (Database ID `(default)`, since
  `FirebaseFirestore.getInstance()` is called with no explicit database name anywhere in the
  code); added a `firestore` section to `firebase.json` pointing at it so the CLI picks it up.
  Verified live via the deploy log ("released rules ... to cloud.firestore") and by reading the
  `trips` collection directly with a developer-supplied service-account key (used once for this
  check, then deleted).
Step 3.4 fully verified after these fixes: manual sync now succeeds and round-trips real data to
Firestore (confirmed a trip document with the correct `collaborators` map), and all delete
confirmations tested per the block's checklist. Block 3 complete.

Block 4 (Structured Flight & Lodging Event Fields) [CODE COMPLETE, pending device check]:
- `AddEventDialog` (OpenedTripScreen.kt) is now category-conditional: Flight relabels
  Start/End Date+Time to Departure/Arrival Date+Time and adds optional Flight number, Departure
  airport code, Arrival airport code fields; Lodging relabels Start/End Date to Check-in/Check-out
  Date and adds an optional Booking number field. All other categories unchanged. Threaded through
  `OpenedTripViewModel.createEvent()` -> `EventRepository.createEvent()` -> `EventEntity`
  (all four fields already existed on the entity/schema from Block 1, just unused until now).
- `OpenedEventScreen.kt`'s edit form gained the same four fields, shown/editable when the event's
  category is Flight/Lodging respectively, alongside the existing fields; threaded through
  `OpenedEventViewModel.updateEvent()` the same way as every other editable field.
- `SyncWorker.kt`'s event upload map and download parsing (both the new-local-row and
  newer-remote-wins-conflict paths) now carry `flightNumber`/`departureAirportCode`/
  `arrivalAirportCode`/`bookingNumber` — previously these Block-1 schema columns were never
  actually written to or read from Firestore, so they'd have been silently dropped on every sync.
- Itinerary tab: `ItineraryEvent` (OpenedTripViewModel.kt) gained `isFirstDay`/`isLastDay` booleans
  (computed in `buildItinerary` from each day slot vs. the event's start/end date; always true/true
  for single-day events) so `EventCard` can tell which side of a multi-day span a given day-card is
  on. `EventCard` (OpenedTripScreen.kt) now gives Flight events a distinct badge ("Departing"/
  "Arriving" instead of "Day X of Y") and an extra flight-info line: same-day shows
  "<number> · <dep> → <arr>", the departure-day card shows only "<number> · <dep>", the arrival-day
  card shows only "<number> · <arr>" — each part omitted if not set, per description_detail.txt §10
  (fields are optional). Lodging and all other categories keep the existing generic card (no
  booking number shown in the list, matching Step 4.3's explicit scope).
- `./gradlew assembleRelease` succeeds from a clean baseline through all these changes.

Developer confirmed on-device, all four Step 4.4 checklist items: same-day Flight event card
(single card, flight number + both airport codes), split-day Flight event cards (departure-day
card shows number + departure code only, arrival-day card shows number + arrival code only),
Lodging booking number (editable on Add Event + Opened Event, list card stays generic with no
booking number shown), and the sync round-trip (flight/lodging fields survive edit + sync +
re-view on a second device/fresh install). Block 4 complete.

Block 5 (Pkpass Note Type) [CODE COMPLETE, pending device check]:
- `pkpass/PkpassContent.kt` (`PkpassContent`/`PkpassField`, `kotlinx.serialization`-annotated) and
  `pkpass/PkpassParser.kt` implement Step 5.1: unzips a picked `.pkpass` via `ZipInputStream`,
  reads `pass.json`, flattens whichever of `primaryFields`/`secondaryFields`/`auxiliaryFields`/
  `backFields` the pass style object (`boardingPass`/`coupon`/`eventTicket`/`generic`/`storeCard`)
  defines into one `fields` list, and picks the first entry of `barcodes` (falling back to the
  legacy singular `barcode` key) for `barcodeMessage`/`barcodeFormat` (normalized to
  QR/PDF417/AZTEC/CODE128, per datastructure.txt §3). Also provides `buildStoragePath`
  (`pkpass/{tripId}/{noteId}/original.pkpass`), `queryDisplayName` (via
  `OpenableColumns.DISPLAY_NAME`), `copyToLocalStorage` (app-internal
  `filesDir/pkpass/{noteId}/original.pkpass`), and `extractStoragePath` (pulled back out of a
  persisted note's `content` JSON string, used by the sync worker). `storage.rules` added at repo
  root (mirrors `firestore.rules`' collaborator/co-owner checks via `firestore.get(...)` from
  within Storage rules) and wired into `firebase.json`'s new `storage` section — needs
  `firebase deploy --only storage` from the developer (same manual-deploy pattern as Phase 2
  Hosting / the Phase 3 Block 3 Firestore rules fix).
- `NoteType.kt` gained `classifyNoteUrl()` (Step 5.3): classifies a pasted URL into
  `GOOGLE_DOC`/`GOOGLE_DRIVE`/`WEB_URL` by substring match on
  `docs./sheets./slides.google.com` vs `drive.google.com`, else `WEB_URL` — same rule the old
  three-way picker relied on, just centralized.
- `AddNoteDialog` (both `OpenedTripScreen.kt` for trip-level notes and `OpenedEventScreen.kt` for
  event-linked notes — kept as two near-identical private composables, matching how each screen
  already had its own copy pre-Block-5) replaces the raw `NoteType` picker with a 4-entry `NoteKind`
  picker (Text Block / Checklist / Link / Pass) per designmockups/AddNote.dc.html (Step 5.2/5.3):
  * Link shows one URL field with a live "Detected automatically as: ..." caption from
    `classifyNoteUrl()`; the resulting `NoteType` + raw URL become the note's `content`.
  * Pass shows a "Choose .pkpass file" button (`ActivityResultContracts.GetContent()`, `"*/*"` —
    `.pkpass` has no registered Android MIME type so extension-based filtering isn't available via
    this contract) that, on pick, runs `PkpassParser.parse()` + `copyToLocalStorage()` on
    `Dispatchers.IO`, pre-fills the title from the pass's `description` if blank, and disables the
    button with a "Reading pass..." label while parsing. A client-generated `noteId` (`remember`ed
    UUID) is used for both the deterministic `storagePath` and the note's actual `id`, so
    `content.storagePath` is correct before the note entity even exists.
  * `NoteRepository.createNote()` gained `localAttachmentPath`/`id` parameters (defaulting to a
    fresh UUID, so every other caller is unaffected) so the dialog's pre-generated ID/local file
    path flow through to the `NoteEntity` row unchanged; threaded through
    `OpenedTripViewModel.createNote()` and `OpenedEventViewModel.addNote()` the same way.
- `SyncWorker.kt` (Step 5.4): on `PENDING_INSERT`/`PENDING_UPDATE` of a `PKPASS` note with a
  non-null `localAttachmentPath`, uploads that file to `FirebaseStorage` at `content.storagePath`
  (extracted via `PkpassParser.extractStoragePath`) *before* writing the Note document; the note
  is only marked `SYNCED` (and its local cached file deleted, `localAttachmentPath` cleared) once
  both the Storage upload and the Firestore write succeed — if the Storage upload throws, the
  Firestore write still proceeds (idempotent `.set()`) but the note stays in its pending state so
  the next sync pass retries just the attachment. On `PENDING_DELETE` of a `PKPASS` note, deletes
  the Storage object at `content.storagePath` after the Firestore delete succeeds, best-effort
  (failure is logged, not retried, per datastructure.txt §5).
- `OpenedNoteScreen.kt` (Step 5.5): `PkpassCard` composable renders organization name, pass
  type/description, the flattened `fields` in a 2-column grid, and a barcode (ZXing
  `MultiFormatWriter` → `BitMatrix` → manually-painted `Bitmap` → `Image`) on a white card matching
  designmockups/PkpassView.dc.html; the note-type badge above the card is suppressed for `PKPASS`
  (the wallet card itself conveys the type). A `DisposableEffect` keyed on `note?.type` sets
  `window.attributes.screenBrightness = 1f` while a Pkpass note is open and restores
  `BRIGHTNESS_OVERRIDE_NONE` on leaving (a `findActivity()` `ContextWrapper`-walk extension gets
  the `Activity`/`Window` from the Compose `LocalContext`).
- `com.google.zxing:core:3.5.3` added to `app/build.gradle.kts`.
- Per the developer: `./gradlew assembleDebug` failed when attempted inside this container
  (environment/tooling issue, not investigated further since builds aren't done in-container —
  see the note below); the developer built it themselves outside the container and confirmed it
  succeeds.

`storage.rules` deployed: `firebase deploy --only storage` against project `tripplanner-a6078`
("released rules storage.rules to firebase.storage", deploy console link confirmed). The
container's cached `firebase-tools` login had gone stale; interactive `firebase login --reauth`
also failed in-container (`auth.firebase.tools/attest` unreachable — a known firebase-tools
device-attestation issue in sandboxed environments), so the developer ran the login flow
themselves and pasted the resulting session back in. (A developer-supplied service-account key
was tried first as a fallback, per the Block 3 pattern, but that key's default
`firebase-adminsdk-fbsvc@...` service account lacks the `firebaserules.releases.create` /
`serviceusage` IAM permissions rules deploys need — deleted immediately after use, never
committed.) `firestore.rules` is unchanged since Block 3, so nothing to redeploy there.

Not yet done — Step 5.6 (Block Verification), which needs a developer device/second-device pass:
import a sample `.pkpass` as a note and confirm title/sync/Firestore+Storage doc; open it and
confirm the barcode scans, fields display, and brightness raises/restores; open the same note on
a second device/account without re-importing; delete it and confirm both the Firestore doc and
Storage object are gone; and paste a Google Doc URL, a Drive URL, and a plain web URL in turn to
confirm each auto-classifies correctly.

Note: Do not make builds. Everything is built outside the container

======================================================================
Phase 4 (Bug Fixes & Small Extensions) started at the developer's request, even though Phase 3
Block 5's Step 5.6 on-device verification (above) is still outstanding — the two are independent
and not blocking each other.

Phase description review step: read DesignDocs/Phase4/description.txt (7 bugs) and
DesignDocs/Phase4/extensions.txt (3 extensions, added by the developer just now). Researched
current behavior for every item before asking questions (date pickers have no trip-scoped
default/bound anywhere; no text field sets KeyboardCapitalization; Add Note never navigates
anywhere after creating; EventEntity has one shared `timezone` column; the trip-default-timezone
bug is already implemented from Phase 3; TripEntity has no image field and Phase 1's
techstack.txt planned one (Google Places Photos + Unsplash fallback + user override + offline
templates, `coil-compose` added for it) that was never built; there's no trip date-editing UI
despite a Phase 3 `TripSettings.dc.html` mockup existing (it only ended up covering the
default-timezone dialog); Phase 3's own techstack.txt §5 already investigated and rejected the
Drive-SAF-picker premise in extensions.txt item A; the Pkpass "coming soon" report in extensions.txt
item C is stale, already fixed in the "Phase 3: Block 5" commit).

Asked and got answers on the 4 genuinely open questions:
* Bug 1 (date picker "start of trip"): default/pre-scroll only, not a hard minimum — a hard bound
  would conflict with Bug 7's "allow out-of-range events, just flag them."
* Bug 2 (auto-capitalize scope): all free-text fields, excluding structured/code-like ones
  (flight number, airport codes, booking number, URLs).
* Bug 6 (trip image): build the full original Phase 1 plan (Places API primary, Unsplash
  fallback, user-upload override, offline illustration templates as last resort) rather than a
  cut-down version.
* Extension A (Drive SAF picker): developer chose to build it anyway despite the verified
  limitation, explicitly as single-device/account convenience only (not cross-collaborator
  sharing) — the UI must make this limitation clear rather than silently failing on another
  device.

Wrote DesignDocs/Phase4/description_detail.txt covering all 7 bugs + 3 extensions with the above
scope decisions. Extension C requires no implementation (already fixed) — will be marked
`[COMPLETED]` in extensions.txt with that explanation once the implementation step begins, no
separate build/verify step for it. Pending developer confirmation of description_detail.txt
before moving to the Tech Stack step.

Before that confirmation, the developer asked to check Phase 1-3 for other planned-but-unexecuted
work (the same class of gap as Bug 6's trip photos). Audit found two:
* Phase 1 techstack.txt §6-7 proposed four external data services (destination/location
  autocomplete via Google Places/Mapbox, weather via WeatherAPI.com/OpenWeatherMap, currency
  conversion via ExchangeRate-API, real-time flight tracking via Aviation Edge API) plus the
  networking client needed to call them (Retrofit/Ktor) — none were ever built, not in any of the
  9 Phase 1-3 implementation-plan blocks, and never marked cut/out-of-scope anywhere. The app
  makes zero external REST calls today (Firebase SDK calls only).
* Phase 2 Block 5 only produced a publishing *manual* — implementation_plan.txt's own Block 5
  verification step says the developer executes it "at their own pace, outside this plan," and
  there was no record (verification report or progress.txt entry) confirming the Play Console
  app/testers were actually set up.

Developer's decisions: the Phase 1 gap is logged as a new placeholder phase (DesignDocs/Phase5/
description.txt, not started, normal phase process applies when picked up) rather than folded
into Phase 4's scope — genuinely new features/dependencies, not bug fixes. Phase 2's publishing
was in fact done by the developer outside the tracked process — confirmed complete, no further
action.

Note: DesignDocs/Phase4/description.txt already exists (a bug list, staged in git as a rename
from DesignDocs/Phase3/bugs.txt) — this is future work, not touched yet.

Developer merged the 3 extensions.txt items into description.txt directly (extensions.txt is now
empty, present but unused) — description.txt is 10 items total, processed as one list with no
bugs-vs-extensions split. Also updated the pkpass item's wording to add "and I cannot choose a
file" — a distinct, not-yet-confirmed-stale symptom alongside the already-source-verified-fixed
"rendering coming soon" text, so description_detail.txt §11 no longer assumes that item needs zero
code work; its implementation step starts by reproducing on a fresh build/install before deciding.
description_detail.txt updated to match (structural intro + §9/§11 wording), content otherwise
unchanged — developer confirmed the items themselves were already correct. Proceeding to the Tech
Stack step.

Wrote DesignDocs/Phase4/techstack.txt. Only item 6 (trip cover images) needs new
services/dependencies — everything else reuses infrastructure already in the app (Room, Firebase
Storage, the Phase 3 Pkpass pick-file/upload/sync pattern, Compose/activity-compose APIs). Key
decisions: Google Places API (New) Text Search + Place Photo endpoints as the primary photo
source, Unsplash Search API as fallback, plain OkHttp + kotlinx.serialization for those two calls
(not Retrofit — deliberately deferred until Phase 5's broader external-service set, if picked up,
justifies it), Android Photo Picker (`PickVisualMedia`, already available via activity-compose
1.8.2) for the user-upload override, and built-in vector-icon/gradient illustrations (no new
assets/dependency) for the offline fallback. Both API keys are developer-provisioned secrets, not
committed. One small new plumbing piece flagged for item 9 (File notes): a `FileProvider` needs
configuring (manifest `<provider>` + `file_paths.xml`) to hand downloaded files to
`ACTION_VIEW`/external apps — no new Gradle dependency, `androidx.core` already has it. Developer
said "proceed" without further comment — treated as confirmation, moved to Data Structure step.

Wrote DesignDocs/Phase4/datastructure.txt. Key decisions:
* Event timezone split (item 4): `timezone` column is kept as-is physically and just renamed to
  `startTimezone` in Kotlin (`@ColumnInfo(name = "timezone")`) — zero new column, zero Firestore
  key change for the start half. Only `endTimezone` is new (column + Firestore key), backfilled
  from `timezone` in the migration so every existing event's behavior is unchanged until edited.
  Download falls back to `"timezone"` when `"endTimezone"` is absent, so pre-Phase-4-synced event
  docs keep working with no server-side migration.
* Trip cover photos (item 6): three new nullable trip columns — `coverPhotoStoragePath` (synced,
  deterministic `trips/{tripId}/cover.jpg`), `coverPhotoSource` (synced, AUTO_PLACES/
  AUTO_UNSPLASH/USER, so the auto-fetch job never clobbers an already-set photo), and
  `localCoverPhotoPath` (local-only staging column, mirrors NoteEntity's `localAttachmentPath`).
  Important call-out: auto-fetched photos (Places/Unsplash) get re-uploaded to our own Firebase
  Storage rather than storing the provider's URL directly — Places' Photo Media URL requires the
  API key on every request, so storing it in a synced Firestore doc would leak the key to every
  collaborator's device; downloading once and re-hosting avoids that.
* New `FILE` note type (item 9): reuses Phase 3's `localAttachmentPath` column as-is (broadened
  comment, no new column) and generalizes `SyncWorker`'s existing PKPASS-only upload/download/
  delete branches to `note.type in (PKPASS, FILE)` — no new sync code path.
* Drive SAF picker (item 8): no new column/type — `GOOGLE_DRIVE` notes' existing `content` string
  field just gains a second possible shape (a small JSON object with `uri`/`displayName` instead
  of a plain URL string), distinguished at render time by attempting a JSON parse first, same
  pattern PKPASS/FILE already use to store structured content.
* Two new Storage rule blocks (`/trips/{tripId}/{fileName}`, `/files/{tripId}/{noteId}/{fileName}`)
  added to the existing storage.rules, reusing its already-defined helper functions — needs a
  `firebase deploy --only storage` once implemented.
* Room migration version 2 -> 3, purely additive/rename, no data loss, no destructive changes.

Developer said "proceed" — treated as confirmation, moved to the Mockups step.

Built DesignDocs/Phase4/designmockups/ via the /design skill's canvas (same approach as Phase 3 —
this environment has no direct image-generation tool), reusing Phase 3's real palette/typeface
(Color.kt/Type.kt) and, for screens being extended rather than introduced, the real Phase 3
.dc.html markup itself (AddNote.dc.html, AddEventFlight.dc.html, TripSettings.dc.html) and
HomeScreen.kt's actual LargeTripCard/SmallTripCard treatment as the starting point. 7 artboards,
matching Phase 3's scope: Home w/ cover photos (auto-fetched + illustration-fallback states), Add
Trip photo section, Trip Settings expanded to a full screen with editable dates + photo override,
Add Event with the new dual-timezone fields, an invalid-event badge + fix-it banner, Add Note with
the new File option + Drive-picker button, and Opened Note states for File/Drive notes. Published,
exportable canvas at https://claude.ai/code/artifact/3cfa6d46-c541-4f02-9fdc-88abe9f86ecd; a
background review pass (checking the working .dc.html files against the brief and the real app
source) was kicked off after publishing, not yet back. Pending developer confirmation of the
mockups before moving to the Implementation Plan step.

Redone at the developer's request: "DO NOT upload anything. Only local png allowed" — the canvas
approach above published a link to claude.ai, which the developer explicitly ruled out. Replaced
with 7 flat PNGs (same screens/scope as above) rendered entirely locally: installed Pillow via apt
(python3-pil, a local package install, not an upload) and wrote a small script that draws each
screen directly as a bitmap — colors/type read from the real Color.kt/Type.kt (DejaVu Sans
standing in locally for the app's Google-Fonts-loaded Outfit), simplified hand-drawn icons in
place of the exact vector glyphs. Iterated on layout by rendering and visually reading back each
PNG (a few overflow/spacing bugs fixed this way: a caption running off the Trip Settings timezone
card, an empty stray field next to Flight number, the Add Note type-picker list box being cut a
row short). Old .dc.html/canvas.json/exported-canvas-html files and the published-link reference
removed from designmockups/ and its README; nothing from this pass was uploaded or published
anywhere. Developer confirmed ("ok, proceed"). Mockups step complete.

Step: implementation plan — written, pending developer confirmation
Wrote DesignDocs/Phase4/implementation_plan.txt (8 blocks) after reading the actual app source
(entities, repositories, ViewModels, SyncWorker, OpenedTripScreen/OpenedEventScreen/HomeScreen,
MainActivity nav, PkpassParser) rather than working from the design docs alone, per instructions.txt's
"detailed enough to resume with only design docs and code as context." Block order: 1 (schema/deps/
FileProvider/API-key scaffolding, no behavior change), 2 (bugs 1-3: date-picker default,
auto-capitalize, add-note-opens-editor — bundled, all trivial/independent), 3 (bug 4-5: dual event
timezones), 4 (bug 7: Trip Settings dialog -> full screen with editable dates + invalid-event
flagging), 5 (bug 6: trip cover photos — Places/Unsplash/manual override/illustration fallback), 6
(extension A: Drive SAF picker), 7 (extension B: File note type), 8 (extension C: pkpass picker bug
— reproduce first, fix only if it reproduces).

Three things the research surfaced are flagged at the top of the plan for the developer to
confirm/override, each already resolved with a documented default rather than left open:
* `trip.defaultTimezone` (a real Phase 3 column) is never actually included in SyncWorker's trip
  upload/download maps — a genuine pre-existing cross-device sync gap, not one of the 10 listed
  items. Fixed opportunistically in Block 5 Step 5.4, which already touches that same map for the
  new cover-photo fields.
* `buildItinerary()`'s day range is bounded strictly to the trip's own dates, so an event entirely
  outside that range (which Bug 1 now explicitly allows creating) would have no day slot to render
  under and be unreachable in the UI — Block 4 Step 4.3 extends the day range to cover such events
  too, as a precondition for the invalid-flagging feature to be reachable at all.
* No event field is timezone-converted for display anywhere in the app today (times render exactly
  as typed). Block 3 is scoped to two independently editable/synced fields, not new conversion
  display logic — flagged so this isn't mistaken for a missed requirement.

Developer confirmed ("ok, proceed"). implementation_plan.txt step complete.

Step: implementation - Block 1 code complete, pending developer build/device verification
implementation_plan.txt (8 blocks) confirmed by developer; implementation started.

Block 1 (Build Environment, New Dependency & Schema Scaffolding) [CODE COMPLETE, pending device
check]:
- Added `com.squareup.okhttp3:okhttp:4.12.0` to `App/app/build.gradle.kts`.
- Schema: `EventEntity.timezone` renamed to `startTimezone` (`@ColumnInfo(name = "timezone")`,
  same physical column) + new nullable `endTimezone`; `TripEntity` gained nullable
  `coverPhotoStoragePath`/`coverPhotoSource`/`localCoverPhotoPath`; `NoteType` gained `FILE`;
  `NoteEntity.localAttachmentPath`'s comment broadened to cover `FILE` too.
- `AppDatabase.version` bumped 2 -> 3, `MIGRATION_2_3` added (`endTimezone` column + backfill from
  `timezone`, three new `trips` columns) and registered in `DatabaseModule.kt` alongside
  `MIGRATION_1_2`.
- Renaming `EventEntity.timezone` broke every call site that named it: fixed
  `EventRepository.createEvent`, `OpenedEventViewModel.updateEvent`, `OpenedEventScreen.kt`'s
  `LaunchedEffect`, `SyncWorker.kt`'s upload map + both download construction sites (new-row and
  updated-row), and `AppDatabaseTest.kt`'s `buildEvent` helper. Kept every function's *public*
  `timezone: String` param name unchanged for now (Block 3's job to split it into two) — internally
  each now writes `startTimezone = timezone, endTimezone = timezone` (create) or
  `startTimezone = timezone, endTimezone = timezone` (update's `.copy(...)`, so editing via today's
  still-single field keeps both in sync, matching pre-Phase-4 behavior exactly until Block 3 lands).
- Adding `NoteType.FILE` broke 4 exhaustive `when(NoteType)` blocks (`OpenedTripScreen.kt`,
  `OpenedEventScreen.kt`, `OpenedNoteScreen.kt` x2) — added a `Teal200`/`InsertDriveFile` visual
  everywhere, plus a "File rendering coming soon" placeholder in the note-content renderer (same
  temporary-placeholder pattern Phase 3 Block 1 used for `PKPASS`; Block 7 replaces it with the
  real card).
- Added FileProvider plumbing for Block 7 (unused until then): `<provider>` entry in
  `AndroidManifest.xml` (`${applicationId}.fileprovider`) + new `res/xml/file_paths.xml`.
- Added cover-photo API key scaffolding for Block 5 (unused until then): `buildFeatures.buildConfig
  = true`, `PLACES_API_KEY`/`UNSPLASH_ACCESS_KEY` `buildConfigField`s read from `App/local.properties`
  (gitignored, same pattern as `keystore.properties`) with an empty-string default; documented both
  keys as commented-out entries in `local.properties` itself for discoverability.
- Not run in-container per the standing note below — developer builds `./gradlew assembleDebug`
  outside the container.

Block Verification not run in-container. Per developer correction ("Build happens at the end of a
phase, proceed to block 2"): builds/on-device verification for this phase happen once at the end,
not after each block — implementation continues block-by-block without pausing for a build each
time. (Saved as a standing preference in memory for future phases too.)

Block 2 (Quick Independent UI Fixes: Bugs 1-3) [CODE COMPLETE]:
- Bug 1 (date picker defaults to trip start): `DatePickerField` gained `defaultMillis: Long? = null`,
  used only when the field's own `value` is blank (`initialMillis ?: defaultMillis` in
  `rememberDatePickerState`). Added a shared `isoDateToEpochMillis()` helper (also used to simplify
  `DatePickerField`'s own existing `initialMillis` computation). Wired trip-start millis into:
  `AddEventDialog`'s both date fields and `AddReminderDialog`'s date field (both copies, in
  `OpenedTripScreen.kt` and `OpenedEventScreen.kt`). `OpenedEventScreen.kt`'s copy needed
  `OpenedEventUiState` to expose the event's trip — added `trip: TripEntity?` there (new `tripFlow`
  in `OpenedEventViewModel.kt`, `roleFlow` now derived from it instead of its own separate query;
  `combine()` widened from 4 to 5 flows). This same `uiState.trip` is reused by Block 4's fix-it
  banner, per the plan. Not applied to `AddTripDialog` (no trip yet) or any edit-form date field
  (always has an existing value already, so the blank-field condition never fires there).
- Bug 2 (auto-capitalize first letter): `TripTextField` (`OpenedTripScreen.kt`'s shared wrapper)
  gained `capitalizeSentences: Boolean = true`, set `false` on its 4 structured-field call sites
  (Flight number, Departure/Arrival airport code, Booking number) and its Email field
  (`ShareTripDialog`). Every other free-text field across `HomeScreen.kt` (Destination),
  `OpenedEventScreen.kt` (Title/Location/Description edit fields, its own Add Note dialog's Title,
  its own Add Reminder dialog's text), `OpenedNoteScreen.kt` (title `TextField`, Text Block content,
  new-checklist-item field), `OpenedReminderScreen.kt` (Reminder Description), and
  `SettingsScreen.kt` (Template name, Items) got `KeyboardOptions(capitalization =
  KeyboardCapitalization.Sentences)` added directly. Left plain (per description_detail.txt §3):
  both note dialogs' URL field, `OpenedEventScreen.kt`'s Flight number/airport codes/Booking number
  edit fields, `OpenedNoteScreen.kt`'s Link-URL edit field, `SettingsScreen.kt`'s Inner Circle email
  field (Inner Circle turned out to be email-only — no separate name field exists).
- Bug 3 (Add Note opens its editor immediately): one-line fix at both `AddNoteDialog` `onConfirm`
  call sites (`OpenedTripScreen.kt`/`OpenedEventScreen.kt`) — `onNoteClick(id)` added after the
  existing `viewModel.createNote(...)`/`addNote(...)` call, using the note's already-known
  client-generated id. No ViewModel/repository change needed, confirming the plan's read that the
  id is available synchronously.
- Not built/run in-container per the above; developer verifies at end of phase.

Block 3 (Per-Event Dual Timezones: Bugs 4-5) [CODE COMPLETE]:
- Data layer: `EventRepository.createEvent`'s `timezone` param split into `startTimezone`/
  `endTimezone` (both default "UTC"); `OpenedTripViewModel.createEvent` same split, passed through;
  `OpenedEventViewModel.updateEvent`'s `timezone` param split the same way. `SyncWorker.kt`'s event
  upload map gains `"endTimezone" to event.endTimezone` alongside the existing `"timezone"` key;
  both download construction sites read a new `remoteEndTimezone = eventDoc.getString("endTimezone")
  ?: remoteTimezone` (the pre-Phase-4-document fallback) and set both fields.
- `AddEventDialog` (`OpenedTripScreen.kt`): single `timezone`/`showTimezonePicker` state replaced
  with `startTimezone`/`endTimezone` + two independent `TimezonePickerDialog`-backed fields, both
  seeded from the dialog's existing `initialTimezone` param (so Bug 5's trip/global default
  resolution — unchanged — now seeds both). Labeled "Departure timezone"/"Arrival timezone" for
  Flight, "Start timezone"/"End timezone" otherwise. `onConfirm`'s lambda gained the 12th param;
  its call site in `OpenedTripScreen`'s main composable and `viewModel.createEvent(...)` updated to
  match.
- `OpenedEventScreen.kt`'s edit form: duplicated the inline `ExposedDropdownMenuBox` timezone
  picker into two (`isStartTimezoneExpanded`/`isEndTimezoneExpanded`, separate search-query/
  filtered-list state per field), same Flight-conditional labeling as the Add Event form, seeded
  from `event.startTimezone`/`event.endTimezone ?: event.startTimezone` in the `LaunchedEffect`,
  threaded into `viewModel.updateEvent(...)`'s new two-param call.
- Verified with a repo-wide grep that no stale single-`timezone` reference remains anywhere the
  rename/split touched.
- Not built/run in-container; developer verifies at end of phase (per the standing note above).

Block 4 (Trip Settings Screen — Editable Dates & Invalid-Event Flagging: Bug 7) [CODE COMPLETE]:
- New `ui/trip/TripSettingsScreen.kt`, modeled on `SettingsScreen.kt`'s full-screen Scaffold/
  TopAppBar/back-arrow pattern, reusing `OpenedTripViewModel` (same `tripId`-scoped `hiltViewModel()`
  pattern `OpenedTripScreen` already uses). New `Screen.TripSettings` route
  (`"tripSettings/{tripId}"`) in `MainActivity.kt`. The old `TripSettingsDialog` AlertDialog and its
  `showTripSettingsDialog` state removed entirely from `OpenedTripScreen.kt`; its default-timezone
  content moved into the new screen unchanged (same `onSetTimezone`/`onClearOverride` calls). The
  gear icon now takes a new `onTripSettingsClick: () -> Unit` screen param, wired in
  `MainActivity.kt` to navigate. Cover photo section stubbed ("Coming soon.") for Block 5.
- Editable trip dates: two `DatePickerField`s save immediately (matching the app's existing
  immediate-commit settings pattern, no separate Save button) via a new
  `OpenedTripViewModel.updateTripDates(startDate, endDate)`, with inline start≤end validation
  (duplicated from `HomeScreen.kt`'s private `validateTripInput`, which isn't cross-file
  accessible).
- Invalid-event detection: new top-level `isEventInvalid(event, trip): Boolean` in
  `OpenedTripViewModel.kt` (pure `LocalDate` comparison, not stored). `ItineraryEvent` gained
  `isInvalid`, set in `buildItinerary`. **Day-range fix** (flagged in the plan's own notes):
  `buildItinerary`'s day range extended from strictly `trip.startDate..trip.endDate` to the union
  of that range and any event's own out-of-range dates, so such an event still gets a day slot to
  render under instead of being unreachable.
- Invalid badge: `EventCard` (`OpenedTripScreen.kt`) shows an "Outside trip dates" pill (ErrorRed,
  warning icon) alongside the existing category/flight badge when `isInvalid`.
- Fix-it banner: `OpenedEventUiState.trip` (added in Block 2) plus the new `isEventInvalid` drive a
  new `InvalidEventBanner` composable in `OpenedEventScreen.kt`, shown as the first `LazyColumn`
  item when the open event is invalid. "Edit event dates" scrolls the list to the form section
  (`animateScrollToItem`, dates are near its top); "Edit trip dates" navigates to the new Trip
  Settings screen via a new `onOpenTripSettings: (String) -> Unit` screen param (wired in
  `MainActivity.kt`).
- Narrowing trip dates needs no extra code: `updateTripDates` only ever touches the trip row, and
  the invalid flag is derived, not stored — confirmed by design, not a separate step.
- Verified with a repo-wide grep that every call site of the changed/new functions
  (`OpenedTripScreen`, `OpenedEventScreen`, `isEventInvalid`, `updateTripDates`) is consistent.
- Not built/run in-container; developer verifies at end of phase.

In-container release build attempted at developer's request (overriding the "builds happen outside
the container" default for a one-off check). First `./gradlew assembleRelease` attempt (12m33s)
caught a real bug: `TripSettingsScreen.kt`'s new `tripSettingsFieldColors()` helper called
`OutlinedTextFieldDefaults.colors(...)` (a @Composable API) without a `@Composable` annotation on
itself — fixed (matches the `@Composable private fun tripTextFieldColors()` pattern every other
screen in this codebase already uses for the same helper). Second attempt (23m12s) got all the way
through compilation, dexing, and release-signing validation — no further code errors — but failed
at the final `:app:packageRelease` task with "Cannot access output property 'incrementalFolder' ...
Could not read directory path ... Accessing unreadable inputs or outputs is not supported." The
directory in question is a perfectly normal, readable directory by `ls`/`stat`. A third attempt with
`--no-watch-fs` (targeting Gradle's file-system watcher as the likely culprit) failed the same way
but on a *different* task/directory (`generateReleaseBuildConfig`'s `sourceOutputDir`, this time) —
confirming this is a nondeterministic, container-level Gradle issue (its output-tracking can't
reliably watch/read this environment's filesystem), not a code problem. This directly matches the
"gradle failed when attempted inside this container (environment/tooling issue)" note from Phase 3
Block 5 — now reproduced and diagnosed further, still not resolved. No release APK produced (the
one sitting in `app/build/outputs/apk/release/` is stale, dated Aug 26, from an earlier session/
build outside this container). Per the developer ("no need to build after that"), not chasing this
further — proceeding to Block 5 without another in-container build.

Not yet started: Block 5 (trip cover images — Places/Unsplash/manual override/illustration
fallback; the biggest remaining block), Block 6 (Drive picker), Block 7 (File note type), Block 8
(pkpass picker bug reproduce/fix).
